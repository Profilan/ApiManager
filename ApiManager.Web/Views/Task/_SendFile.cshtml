@model ApiManager.Web.Models.TaskViewModel


<div class="form-group row">
    <div class="col-lg-6">
        @Html.LabelFor(model => model.SenderViewModel.FileViewModel.Formats)
        @Html.ListBoxFor(model => model.SenderViewModel.FileViewModel.SelectedFormats, Model.FileViewModel.Formats, new { @class = "form-control" })
    </div>
</div>

<div class="form-group">
    <label>Shares</label>
    <div class="kt-checkbox-list">
        @foreach (var share in Model.SenderViewModel.FileViewModel.Shares)
        {
            var selected = "";
            if (Model.SenderViewModel.FileViewModel.SelectedShares.Contains(share.Id))
            {
                selected = "checked";
            }
            <label class="kt-checkbox">
                <input type="checkbox" id="SenderViewModel_FileViewModel_SelectedShares" name="SenderViewModel.FileViewModel.SelectedShares" value="@share.Id" @selected>
                @share.Name
                <span></span>
            </label>

        }
    </div>
</div>
